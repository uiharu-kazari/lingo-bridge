# Models & generated audio (large / reproducible)
models/
audio_cache/
*.gguf

# Python
__pycache__/
*.pyc

# Node / puppeteer screenshot tooling (dev only)
node_modules/
package.json
package-lock.json

# Scratch experiments, logs, screenshots (all _-prefixed)
_*

# Demo recording intermediates (final video lives in docs/demo.mp4)
video_raw/
demo_audio.wav
sil.wav

# AI-tool integration artifacts (not part of the app)
.claude/
.agents/
skills-lock.json

# OS
.DS_Store

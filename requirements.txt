# Thin Gradio Space: serves the UI + proxies model calls to the Modal GPU
# backend (Qwen3-4B via llama.cpp + OpenBMB VoxCPM2). No models run on the
# Space, so no llama-cpp-python / torch / voxcpm here — those live in modal_app.py.
gradio>=5.50,<6
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
huggingface_hub>=0.24,<1.0.0
numpy>=1.24,<2.0.0
